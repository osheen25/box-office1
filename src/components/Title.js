import React from 'react'

const Title = ({title , subtitle}) => {
    return (
        <div>
        <h1>{title}</h1>
        <p1>{subtitle}</p1>
            
        </div>
    )
}

export default Title
